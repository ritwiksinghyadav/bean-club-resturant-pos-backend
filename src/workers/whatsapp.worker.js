import { logger } from "../utils/logger.js";

export const startWhatsappWorker = () => {
  logger.info("🤖 WhatsApp Worker (Mocked/Disabled for Phase 1) started.");
  return null;
};

export const stopWhatsappWorker = async () => {
  logger.info("🤖 WhatsApp Worker (Mocked/Disabled for Phase 1) stopped.");
};

